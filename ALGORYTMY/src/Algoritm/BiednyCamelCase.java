package Algoritm;

/**
 * Created by RENT on 2017-07-21.
 */
public class BiednyCamelCase extends AbstractAlgoritm {
    @Override
    public String getName() {
        return "Biedny Camel Case";
    }

    @Override
    public void runAlgoritm(String[] input) {
        String wyraz = "ToWyraz";
        char[] Tablica = wyraz.toCharArray();
        Character.toString(wyraz.charAt());

        int literki = 0;

        for (int i = 1; i < input.length; i++){
            char[] tablica = input[i].toCharArray();
            System.out.println();
            for (int j=0; j< input[j].toUpperCase().charAt(j));

            if (i %2 == 0);
            System.out.println("Duze litery");
            char[] małe = input[j].toUpperCase().charAt(j);
            if
            else
            System.out.println();

        }
    }
}
